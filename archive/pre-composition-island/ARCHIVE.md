# Pre-Composition-Island Archive

Archived on **2026-07-10** from baseline commit `49a0344`.

This directory preserves the implementation that preceded the composition
island architecture. The archived scope is:

- `packages/editable`
- `src/editable-lab`
- `tests/browser`
- root `src/styles.css`
- root route `src/routes/index.tsx`
- root `README.md` as `README.original.md`

At the baseline, `pnpm exec tsc --noEmit` passed and the core Vitest suite passed
all **96 tests**.

This is reference material only. It is excluded from the active build, typecheck,
test suites, and package surface. It is not maintained, fixed, or kept compatible
with the replacement implementation. Copy ideas deliberately; do not import or
execute files from this archive.
